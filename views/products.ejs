<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Products - Online Pharmacy</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .card-img-top {
      height: 200px;
      object-fit: cover;
    }
    .btn-primary {
      background-color: #ff4081;
      border-color: #ff4081;
    }
    .btn-primary:hover {
      background-color: #e91e63;
      border-color: #e91e63;
    }
    .card-title {
      font-weight: 600;
    }

    /* ✅ Styling for out-of-stock products */
    .out-of-stock {
      filter: grayscale(100%);
      opacity: 0.7;
      position: relative;
    }
    .out-of-stock-label {
      position: absolute;
      top: 10px;
      left: 10px;
      background: black;
      color: white;
      padding: 3px 8px;
      font-size: 12px;
      border-radius: 5px;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <%- include('partials/navbar', { page: 'cart' }) %>

<div class="container mt-5">
  <h2 class="mb-4 text-center">Our Medicines</h2>
  <div class="row">
    <% if (products.length > 0) { %>
      <% products.forEach(product => { %>
        <div class="col-md-4 mb-4">
          <div class="card h-100 shadow-sm <%= product.stock <= 0 ? 'out-of-stock' : '' %>">
            
            <!-- Product image -->
            <img src="/images/<%= product.image %>" class="card-img-top" alt="<%= product.name %>">
            <% if (product.stock <= 0) { %>
              <span class="out-of-stock-label">OUT OF STOCK</span>
            <% } %>

            <div class="card-body">
              <h5 class="card-title"><%= product.name %></h5>
              <p class="card-text text-muted"><%= product.description %></p>
              <p><strong>Category:</strong> <%= product.category %></p>
              <p><strong>Price:</strong> ₹<%= product.price %></p>
              <p><strong>Stock:</strong> 
                <% if (product.stock > 0) { %>
                  <%= product.stock %> available
                <% } else { %>
                  <span class="text-danger">Out of Stock</span>
                <% } %>
              </p>
            </div>

            <div class="card-footer text-center">
              <% if (product.stock > 0) { %>
                <form method="post" action="/cart/add/<%= product.id %>">
                  <input type="hidden" name="name" value="<%= product.name %>">
                  <input type="hidden" name="price" value="<%= product.price %>">
                  <input type="hidden" name="image" value="<%= product.image %>">
                  <input type="hidden" name="stock" value="<%= product.stock %>"> <!-- ✅ FIX -->
                  <button type="submit" class="btn btn-primary">Add to Cart</button>
                </form>
              <% } else { %>
                <span class="badge bg-dark">❌ Out of Stock</span>
              <% } %>
            </div>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <div class="col-12 text-center">
        <div class="alert alert-warning">⚠ No products found!</div>
      </div>
    <% } %>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
